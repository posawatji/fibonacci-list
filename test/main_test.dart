import 'package:flutter_test/flutter_test.dart';
import 'package:fibonacci_list/main.dart' as app;

void main() {
  test('main() runs without errors', () {
    expect(() => app.main(), returnsNormally);
  });
}
